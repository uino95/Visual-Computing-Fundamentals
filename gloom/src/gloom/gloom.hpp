// Preprocessor directives
#ifndef GLOOM_HPP
#define GLOOM_HPP
#pragma once

// System Headers
#include <glad/glad.h>

// Standard headers
#include <string>

// Constants
const int         windowWidth     = 900.0;
const int         windowHeight    = 900.0;
const std::string windowTitle     = "Minecraft";
const GLint       windowResizable = GL_TRUE;
const int         windowSamples   = 4;

#endif
